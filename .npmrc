# .npmrc - npm configuration file
# Registry for publishing packages
registry=https://registry.npmjs.org/

# Enable progress bar for better UX
progress=true

# Set timeout for network requests (60 seconds)
fetch-timeout=60000